<!DOCTYPE HTML>
<html>
   <head>
        <title>EVE</title>
        <link rel="stylesheet" href="/static/style.css">
        <script type="text/javascript" src="/static/ws.js"></script>
		<svg version="1.1" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" class="blur-svg">
			<defs>
				<filter id="blur-filter">
					<feGaussianBlur stdDeviation="3"></feGaussianBlur>
				</filter>
			</defs>
		</svg>
   </head>
   <body>
        <div class="content">
		<div class="chat-list">
			<div class="topBar">
			<div id="title">
				EVE - Datingassistance
			</div>
			<div id="username">
				{{.User.Name}} (Logout)
			</div>
			<div id="newBot">
                <button id="newBotButton" onclick="showPopup()" value=""></button>
			</div>
			</div>
			
        {{if .ActiveBot}}
            <div class="bot-list"><ul>
            {{range $bot := .Bots}}
                <li class="bot" id="{{if eq $.ActiveBot.ID $bot.ID}}bot-active{{end}}" botID="{{$bot.ID}}">
                    <a href="/?bot={{$bot.ID}}"><img src="http://via.placeholder.com/150x150"> {{$bot.Name}}</a>
                </li>
            {{end}}
        {{end}}
            </ul></div>
        </div>
        {{if .ActiveBot}}
        <div class="chat">
            <div class="topBar2">
			<img src="http://via.placeholder.com/150x150">
            <div id="botname">Chatting with: <b>{{.ActiveBot.Name}}</b></div>
			</div>
            <div id="log">
                {{range $msg := .Messages}}
                <p class="msg {{if eq $msg.Sender 1}}user{{else}}bot{{end}}">{{$msg.Content}}</p>
                {{end}}
            </div>
            <form id="form" autocomplete="off">
                <textarea id="msg" maxlength="200"></textarea>
                <input type="submit" value="" id="send"/>
            </form>
        </div>
		</div>
		
        {{end}}
		
		<div id="popup">
		<h1>New chat.</h1>
		<div id="hidePopupButton"><button onclick="hidePopup()"></button></div>
		<div id="">
				
				<div class="formheadermanual">Gender</div><br />
				<div class="switch-field"><input type="radio" id="switch_left" name="gender" value="f" checked />
				<label for="switch_left">Female</label>
				<input type="radio" id="switch_right" name="gender" value="m" />
				<label for="switch_right">Male</label></div>
				<br />
				<div class="formheadermanual">Profile picture</div><br />
				<button id="imageinput" onclick="changeImagePopup()" value="">Select</button>
				<br /><br />
				<div class="formheadermanual">Name of your partner</div><div class="inputcontainer"><input type="text" name="botname" maxlength="30" value="Generated name goes here" readonly /></div>
                <form method="post" action="/createBot">
				<input class="button" type="submit" value="Create new chat"/>
				</form>
		</div>
		</div>
		
   </body>
</html>